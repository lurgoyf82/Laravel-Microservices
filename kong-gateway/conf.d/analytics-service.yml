_format_version: "3.0"

services:
  - name: analytics
    url: http://analytics-service:80
    routes:
      - name: analytics
        paths:
          - /api/v1/analytics
        strip_path: false
        methods: [GET, POST, PUT, PATCH, DELETE]
