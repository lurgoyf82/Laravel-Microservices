_format_version: "3.0"

services:
  - name: notification
    url: http://notification-service:80
    routes:
      - name: notification
        paths:
          - /api/v1/notification
        strip_path: false
        methods: [GET, POST, PUT, PATCH, DELETE]